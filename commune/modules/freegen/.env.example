### Shared settings for docker compose (edit then `docker compose --profile <p> up`)
# Frontend RPC HTTP URL (used in test/main profiles)
EVM_RPC_HTTP=https://base-sepolia.g.alchemy.com/v2/REPLACE_ME
# Chain IDs: Base=8453, Base Sepolia=84532, Mainnet=1, Sepolia=11155111
CHAIN_ID=84532

### LOCAL profile only (hardhat node inside container)
LOCAL_CHAIN_ID=31337

### Optional: auto-exposed seed values (the local bootstrap writes .env.local inside web/)
FACTORY=
REGISTRY=
ADAPTER_CHAINLINK=
ADAPTER_PYTH=
ADAPTER_MOCK=
LOCAL_ORACLE_ID=
