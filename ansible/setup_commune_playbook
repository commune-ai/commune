- name: Transfer and execute a script.
  hosts: all
  tasks:
    - name: Copy and Execute the script 
      script: /Users/salvatore/commune/scripts/deploy_commune.sh
      register: hello
    - debug: msg="{{ hello.stdout }}"

    - debug: msg="{{ hello.stderr }}"